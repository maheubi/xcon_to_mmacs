obstacle_range: 2.5
raytrace_range: 3.0
# use round footprint (due to turns)
robot_radius: 0.4
#footprint: [[0.53, 0.205], [-0.35, 0.205], [-0.35, -0.205],[0.53, -0.205]]

inflation_radius: 0.5

observation_sources: laser_scan_sensor

laser_scan_sensor: {sensor_frame: zed_camera_center, data_type: LaserScan, topic: zed/scan, marking: true, clearing: true}
#laser_scan_sensor: {sensor_frame: velodyne, data_type: LaserScan, topic: /velodyne/scan_costmap, marking: true, clearing: true}
#laser_scan_sensor: {sensor_frame: os_lidar, data_type: LaserScan, topic: /ouster/scan, marking: true, clearing: true}

robot_base_frame: base_link
update_frequency: 4.0
publish_frequency: 4.0
#transform_tolerance: 0.5
transform_tolerance: 1.5


resolution: 0.05

#layer definitions
static:
    map_topic: /map
    subscribe_to_updates: true

